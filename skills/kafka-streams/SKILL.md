# Kafka Streams Skill

Manage Kafka topics, consumers, and stream processing.

## Usage

- `kafka topics list` - List all topics
- `kafka topic create <name>` - Create topic
- `kafka consume <topic>` - Consume messages
- `kafka produce <topic> <message>` - Produce message

## Requirements

- Kafka CLI tools
- KAFKA_BOOTSTRAP_SERVERS environment variable
