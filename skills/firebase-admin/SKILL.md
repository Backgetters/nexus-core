# Firebase Admin Skill

Manage Firebase projects, Firestore, Auth, and Cloud Functions.

## Usage

- `firebase projects list` - List Firebase projects
- `firebase firestore query <collection>` - Query Firestore
- `firebase auth users` - Manage auth users
- `firebase deploy` - Deploy Cloud Functions

## Requirements

- Firebase CLI installed
- GOOGLE_APPLICATION_CREDENTIALS set
