# Sentry Monitor Skill

Monitor errors and performance with Sentry.

## Usage

- `sentry projects list` - List projects
- `sentry issues list` - List unresolved issues
- `sentry issue resolve <id>` - Resolve issue
- `sentry alerts list` - List alert rules

## Requirements

- SENTRY_AUTH_TOKEN environment variable
